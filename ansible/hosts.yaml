development:
  hosts:
    ubuntu-desktop:
  vars:
    node_env: development

staging:
  hosts:
    time-machine:
  vars:
    node_env: development  # FOR NOW.

production:
  hosts:
  vars:
    node_env: production
