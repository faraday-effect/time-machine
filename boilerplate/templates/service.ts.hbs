import { Injectable } from "@nestjs/common";
import { InjectRepository } from "@nestjs/typeorm";
import { {{ name }}, {{ name }}CreateInput } from "./entities";
import { Repository } from "typeorm";

@Injectable()
export class {{ name }}Service {
  constructor(
    @InjectRepository({{ name }}) private readonly {{ lower name }}Repo: Repository<{{ name }}>
  ) {}

  create{{ name }}(createInput: {{ name }}CreateInput) {
    return this.{{ name }}Repo.save(this.{{ lower name }}Repo.create(createInput));
  }

  read{{ namePlural }}() {
    return this.{{ name }}Repo.find();
  }
}
