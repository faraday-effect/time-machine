import { Args, Mutation, Query, Resolver } from "@nestjs/graphql";
import { {{ name }}Service } from "./{{ lower name }}.service";
import { {{ name }}, {{ name }}CreateInput } from "./entities";

@Resolver("{{ name }}")
  export class {{ name }}Resolver {
  constructor(private readonly {{ lower name }}Service: {{ name }}Service) {}

  @Mutation(() => {{ name }})
    create{{ name }}(@Args("createInput") createInput: {{ name }}CreateInput) {
    return this.{{ lower name }}Service.create{{ name }}(createInput);
  }

  @Query(() => [{{ name }}])
    read{{ namePlural }}() {
    return this.{{ lower name }}Service.read{{ namePlural }}();
  }
}
